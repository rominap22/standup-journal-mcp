[project]
name = "standup-journal-mcp"
version = "0.1.0"
description = "Standup checklist of all working tasks using Claude Desktop MCP"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=2.0.0",
]
